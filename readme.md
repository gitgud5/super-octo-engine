If air isn't working, this is a solution

`export PATH="$HOME/go/bin:$PATH"`

And we can permanently add it to PATH using the following command

```bash
echo 'export PATH="$HOME/go/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc
```
